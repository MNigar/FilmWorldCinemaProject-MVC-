@model FilmWorldCinemaProject_MVC_.Models.DbModel.CinemaHall

@{
    ViewBag.Title = "EditHallCinema";
}

<h2>EditHallCinema</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>CinemaHall</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        @*<div class="form-group">
            @Html.LabelFor(model => model.CinemaId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.CinemaId, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.CinemaId, "", new { @class = "text-danger" })
            </div>
        </div>*@
        <input type="hidden" name="CinemaId" value="@Model.CinemaId" />
        <span> @Model.Cinemas.Name</span>
        <div class="form-group">
            <select class="form-control select2" name="halls"  required>
                @foreach (var item in ViewBag.Hall)
                {

                    @*<option value="@item.Id">
                    @item.Janrs.Name
                </option>*@
                    <option @(Model.Halls.Name.FirstOrDefault() !=0 ? "selected" : "") value="@item.Id">
                        @item.Name
                    </option>
                }

            </select>
            
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
