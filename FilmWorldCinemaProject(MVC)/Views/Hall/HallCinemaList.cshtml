@model IEnumerable<FilmWorldCinemaProject_MVC_.Models.DbModel.CinemaHall>

@{
    ViewBag.Title = "HallCinemaList";
}

<h2>HallCinemaList</h2>

<p>
    @Html.ActionLink("Create New", "CreateHallCinema")
</p>
<table class="table">
    <tr>
        <th>
            <span > Cinema</span>

        </th>
        <th>
            <span > Hall</span>

        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cinemas.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Halls.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditHallCinema", new { id = item.Id }) |
            @*@Html.ActionLink("Details", "Details", new { id = item.Id }) |*@
            @Html.ActionLink("Delete", "DeleteHallCinema", new { id = item.Id })|

            @if (ViewBag.Seat.Count != 0)
            {
                foreach (var i in ViewBag.Seat)
                {
                    if (i == item.Id)
                    {

                    }
                    else
                    {
                        <a href="@Url.Action("CreateHallCinemaRow", "Row",new { id = item.Id })"> Add seat</a>

                    }
                }
            }
            else
            { <a href="@Url.Action("CreateHallCinemaRow", "Row",new { id = item.Id })"> Add seat</a>

            }
        </td>
    </tr>
}
    
</table>
